# add all the files that are not needed to be committed
*.pyc
*.pyo
*.pyd
*.pyw
*.pyz
*.pywz

*.sqlite3
*.db
*.log

/dropme_env
# Ignore local .env file (for local development)
.env
# But allow prod_env.txt to be version controlled (it will be copied to .env in container)
!prod_env.txt
